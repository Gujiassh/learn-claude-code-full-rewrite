# 阶段-04 多代理协同

## 目标

让多个 worker 能通过统一消息协议领取任务、汇报完成状态，形成可追踪的协同闭环。

## 已交付

- `src/full_rewrite/team_coordination.py`
  - `TeamMessage`
  - `TeamMailbox`
  - `TeamCoordinator`
  - 能力：任务分配消息、完成回执、增量收件
  - 强化：收件人标识白名单校验，阻断路径穿越写入
  - 强化：任务完成必须由实际 assignee 上报
- `tests/test_team_coordination.py`
- `tests/test_task_board.py`

## 验证

```bash
python3 -m pytest -q
```

## 结果

- 状态：完成
- 下一阶段：阶段-05 上下文治理

## 补充说明（2026-03-05）

- 为避免协同层误报完成，`TaskBoard.complete` 新增 actor 校验路径。
- 为避免 mailbox 文件越界，receiver 只允许 `A-Za-z0-9_.-`。
