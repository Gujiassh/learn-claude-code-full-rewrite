# 阶段-06 安全与权限

## 目标

为工具调用和命令执行提供策略闸门与审计追踪，确保默认安全。

## 已交付

- `src/full_rewrite/security_policy.py`
  - `SecurityPolicy`
  - `PolicyDecision`
  - `AuditTrail`
  - 能力：工具白名单、危险命令阻断、审计记录
- `tests/test_security_policy.py`
- `tests/test_runtime.py`（策略拦截 + 审计联动）

## 验证

```bash
python3 -m pytest -q
```

## 结果

- 状态：完成
- 下一阶段：阶段-07 可观测性
