# 阶段-05 上下文治理

## 目标

把上下文压缩、持久化与恢复做成标准能力，避免会话漂移和上下文爆炸。

## 已交付

- `src/full_rewrite/context_governance.py`
  - `ContextGovernor`
  - `ContextMessage`
  - 能力：历史摘要压缩、最近窗口控制、磁盘保存与恢复
- `tests/test_context_governance.py`
- `tests/test_runtime.py`（`AgentLoop` 与 `ContextGovernor` 集成验证）

## 验证

```bash
python3 -m pytest -q
```

## 结果

- 状态：完成
- 下一阶段：阶段-06 安全与权限
