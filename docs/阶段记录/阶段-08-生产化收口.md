# 阶段-08 生产化收口

## 目标

补齐对外接口、命令入口、烟雾脚本与回归矩阵，完成可交付收口。

## 已交付

- `src/full_rewrite/cli.py`
  - 命令：`task` / `context` / `observe`
- `src/full_rewrite/api.py`
  - `RewriteAPI` 程序化接口
- `pyproject.toml`
  - `rewrite-agent` / `rewrite-api` 两个入口
- `scripts/run_cli_smoke.sh`
- `scripts/run_api_smoke.sh`
- `tests/test_cli.py`
- `tests/test_api.py`

## 验证

```bash
python3 -m pytest -q
bash scripts/run_cli_smoke.sh
bash scripts/run_api_smoke.sh
```

## 结果

- 状态：完成
- 当前阶段：全量重写阶段收口完成
