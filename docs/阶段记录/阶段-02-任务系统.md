# 阶段-02 任务系统

## 目标

把任务拆分与依赖关系做成可持久化能力，支持后续多代理协同与自动领取。

## 已交付

- `src/full_rewrite/task_board.py`
  - `TaskItem`
  - `TaskBoard`
  - 能力：新增任务、依赖判断、领取就绪任务、完成任务、磁盘持久化
- `tests/test_task_board.py`

## 验证

```bash
python3 -m pytest -q
```

## 结果

- 状态：完成
- 下一阶段：阶段-03 背景执行
